# core (training and tests)
torch>=2.1
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
pyyaml>=6.0
pytest>=7.0

# feature extraction (only needed for common/features)
transformers>=4.40
librosa>=0.10
decord>=0.6        # video decoding; torchvision also works
# optional: speechbrain (x-vector), tensorflow + tensorflow_hub (TRILLsson), hear21passt (PaSST)
